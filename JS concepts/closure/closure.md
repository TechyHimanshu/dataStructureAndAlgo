closure  =>  In javascript function having access to outer scope of a function inside the inner functioneven after the outer function has closed.

Closure are nothing but function with preserved data.
